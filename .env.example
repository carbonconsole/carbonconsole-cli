# Copy to .env for local development. NEVER commit .env.
# Get a token from the CarbonConsole dashboard: Settings -> API tokens.
CARBONCONSOLE_API_TOKEN=

# Override the API base URL (optional; defaults to the public API).
CARBONCONSOLE_API_URL=https://api.carbonconsole.com
